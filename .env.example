# Directus Backend URL
# Set this to your Directus instance URL
NUXT_PUBLIC_DIRECTUS_URL=http://localhost:8055

# For production with api.addr.nl:
# NUXT_PUBLIC_DIRECTUS_URL=https://api.addr.nl
#
# Note: Make sure your Directus server has CORS configured to allow
# requests from your frontend origin. In your Directus .env:
#   CORS_ENABLED=true
#   CORS_ORIGIN=https://your-frontend-domain.com
